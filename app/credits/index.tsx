import StyledText from "@/components/elements/StyledText";
import { SafeAreaView } from "react-native-safe-area-context";

// Customer credits screen
export default function Credits() {
    return (
        <SafeAreaView>
            <StyledText>Credits Screen</StyledText>
        </SafeAreaView>
    )
}