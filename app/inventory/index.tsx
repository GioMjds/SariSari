import StyledText from "@/components/elements/StyledText";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Inventory() {
    return (
        <SafeAreaView>
            <StyledText>Inventory Screen</StyledText>
        </SafeAreaView>
    )
}