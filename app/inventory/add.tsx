import StyledText from "@/components/elements/StyledText";
import { SafeAreaView } from "react-native-safe-area-context";

export default function AddInventory() {
    return (
        <SafeAreaView>
            <StyledText>Add Inventory Screen</StyledText>
        </SafeAreaView>
    )
}